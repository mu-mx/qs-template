import { renderHook } from '@testing-library/react-hooks';
import useHiTemplate from '../index';

describe('useHiTemplate', () => {
  it('should be defined', () => {
    expect(useHiTemplate).toBeDefined();
  });

  it('useHiTemplate should work', () => {
    // do something
  });
});
